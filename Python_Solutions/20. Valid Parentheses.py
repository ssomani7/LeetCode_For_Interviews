class Solution:
    def isValid(s):
        st = []
        for each in s:
            if each in ('[','()','{'):
                st.push(each)
            else:
                st.push(each)
    a = isValid("]")
    print(a)